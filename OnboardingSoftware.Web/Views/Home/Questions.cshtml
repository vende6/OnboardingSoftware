@model IEnumerable<OnboardingSoftware.Web.Models.PitanjeViewModel>

@{
    ViewData["Title"] = "Home page";
}
<p style="height: 80px"></p>
<h4>Questions</h4>
<p align="right">
    <input type="button" class="btn btn-light" value="Create" onclick="location.href='@Url.Action("CreateQuestion", "Home" )'" />
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                No.
            </th>
            <th>
                Type
            </th>
            <th>
                Text
            </th>
            <th>
                Test
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(model => item.RedniBroj)
            </td>
            <td>
                @Html.DisplayFor(model => item.Tip)
            </td>
            <td>
                @Html.DisplayFor(model => item.Tekst)
            </td>
            <td>
                @Html.DisplayFor(model => item.Test)
            </td>
        </tr>
        }
    </tbody>
</table>
